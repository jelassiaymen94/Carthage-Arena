<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réinitialisation de mot de passe — Carthage Arena</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #0d0d0d;
            font-family: 'Helvetica Neue', Arial, sans-serif;
            color: #e5e5e5;
        }
        .wrapper {
            width: 100%;
            background-color: #0d0d0d;
            padding: 40px 20px;
        }
        .container {
            max-width: 560px;
            margin: 0 auto;
            background-color: #161616;
            border: 1px solid rgba(255,255,255,0.07);
            border-radius: 20px;
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #1a0a0a 0%, #0d0d0d 100%);
            padding: 36px 40px 28px;
            text-align: center;
            border-bottom: 1px solid rgba(230, 0, 19, 0.15);
        }
        .header h1 {
            margin: 12px 0 0;
            font-size: 22px;
            font-weight: 900;
            letter-spacing: -0.5px;
            color: #ffffff;
            text-transform: uppercase;
            font-style: italic;
        }
        .header h1 span {
            color: #e60013;
        }
        .header .tagline {
            margin: 6px 0 0;
            font-size: 11px;
            color: rgba(255,255,255,0.3);
            letter-spacing: 2px;
            text-transform: uppercase;
        }
        .body {
            padding: 36px 40px;
        }
        .greeting {
            font-size: 18px;
            font-weight: 700;
            color: #ffffff;
            margin: 0 0 16px;
        }
        .text {
            font-size: 14px;
            line-height: 1.7;
            color: rgba(255,255,255,0.6);
            margin: 0 0 28px;
        }
        .btn-wrapper {
            text-align: center;
            margin: 32px 0;
        }
        .btn {
            display: inline-block;
            background-color: #e60013;
            color: #ffffff !important;
            text-decoration: none;
            font-size: 14px;
            font-weight: 700;
            padding: 14px 36px;
            border-radius: 12px;
            letter-spacing: 0.3px;
        }
        .divider {
            border: none;
            border-top: 1px solid rgba(255,255,255,0.06);
            margin: 28px 0;
        }
        .small-text {
            font-size: 12px;
            color: rgba(255,255,255,0.3);
            line-height: 1.6;
            margin: 0;
        }
        .small-text a {
            color: #e60013;
            text-decoration: none;
            word-break: break-all;
        }
        .footer {
            padding: 20px 40px 28px;
            text-align: center;
        }
        .footer p {
            font-size: 11px;
            color: rgba(255,255,255,0.2);
            margin: 0;
            line-height: 1.6;
        }
        .warning-box {
            background: rgba(230, 0, 19, 0.07);
            border: 1px solid rgba(230, 0, 19, 0.15);
            border-radius: 10px;
            padding: 14px 18px;
            margin: 0 0 20px;
        }
        .warning-box p {
            font-size: 12px;
            color: rgba(255,255,255,0.45);
            margin: 0;
            line-height: 1.6;
        }
        .warning-box p strong {
            color: #e60013;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <div class="container">
            <!-- Header -->
            <div class="header">
                <h1>Carthage <span>Arena</span></h1>
                <p class="tagline">Esports Tournament Platform</p>
            </div>

            <!-- Body -->
            <div class="body">
                <p class="greeting">Bonjour, {{ user.name }} 👋</p>

                <p class="text">
                    Vous avez demandé la réinitialisation du mot de passe de votre compte
                    <strong style="color: #fff;">{{ user.email }}</strong>.<br><br>
                    Cliquez sur le bouton ci-dessous pour choisir un nouveau mot de passe.
                    Ce lien est valable pendant <strong style="color: #fff;">{{ expiresInMinutes }} minutes</strong>.
                </p>

                <div class="btn-wrapper">
                    <a href="{{ resetUrl }}" class="btn">
                        Réinitialiser mon mot de passe
                    </a>
                </div>

                <div class="warning-box">
                    <p>
                        <strong>Vous n'avez pas fait cette demande ?</strong><br>
                        Ignorez simplement cet e-mail. Votre mot de passe restera inchangé et aucune action ne sera effectuée.
                    </p>
                </div>

                <hr class="divider">

                <p class="small-text">
                    Si le bouton ne fonctionne pas, copiez et collez ce lien dans votre navigateur :<br>
                    <a href="{{ resetUrl }}">{{ resetUrl }}</a>
                </p>
            </div>

            <!-- Footer -->
            <div class="footer">
                <p>
                    © {{ "now"|date("Y") }} Carthage Arena. Tous droits réservés.<br>
                    Cet e-mail a été envoyé automatiquement, merci de ne pas y répondre.
                </p>
            </div>
        </div>
    </div>
</body>
</html>
