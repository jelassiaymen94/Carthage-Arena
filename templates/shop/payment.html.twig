{% extends 'base.html.twig' %}

{% block title %}Redirection vers Stripe…{% endblock %}

{% block body %}
<div class="min-h-screen bg-background-dark flex items-center justify-center">
  <div class="text-center">
    <svg class="animate-spin w-12 h-12 text-violet-500 mx-auto mb-4" fill="none" viewBox="0 0 24 24">
      <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/>
      <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/>
    </svg>
    <p class="text-white font-semibold">Redirection vers Stripe…</p>
    <p class="text-gray-500 text-sm mt-1">Veuillez patienter.</p>
  </div>
</div>
{% endblock %}
