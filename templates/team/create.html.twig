{% extends 'base.html.twig' %}

{% block title %}Créer une Équipe - Carthage Arena{% endblock %}

{% block body %}
<div class="app-container">
    {% include 'components/_sidebar.html.twig' %}

    <main class="main-content">
         <div style="margin-bottom: 2rem;">
            <a href="{{ path('app_team') }}" style="display: inline-flex; align-items: center; gap: 0.5rem; color: var(--color-text-secondary); font-size: 0.875rem; font-weight: 600;">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M19 12H5M12 19l-7-7 7-7"/>
                </svg>
                Retour
            </a>
        </div>

        <div class="card" style="max-width: 800px; margin: 0 auto;">
            <div class="page-header" style="margin-bottom: 2rem; border-bottom: 1px solid var(--color-border); padding-bottom: 1.5rem;">
                <h1 class="page-title">Créer votre Équipe</h1>
                <p class="page-subtitle">Configurez l'identité de votre équipe.</p>
            </div>

            <form>
                <div class="form-group" style="margin-bottom: 2rem;">
                    <label style="display: block; margin-bottom: 1rem; font-weight: 600;">Logo de l'équipe</label>
                    <div style="display: flex; align-items: center; gap: 2rem;">
                         <div style="width: 100px; height: 100px; border-radius: 50%; background: var(--color-bg-tertiary); border: 2px dashed var(--color-text-secondary); display: flex; align-items: center; justify-content: center;">
                            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="var(--color-text-secondary)" stroke-width="2">
                                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                                <polyline points="17 8 12 3 7 8"></polyline>
                                <line x1="12" y1="3" x2="12" y2="15"></line>
                            </svg>
                         </div>
                         <div>
                            <button type="button" class="btn btn-secondary">Télécharger une image</button>
                            <div style="font-size: 0.75rem; color: var(--color-text-muted); margin-top: 0.5rem;">JPG ou PNG, max 2MB.</div>
                         </div>
                    </div>
                </div>

                <div class="form-group">
                    <label class="form-label">Nom de l'équipe</label>
                    <input type="text" class="form-input" placeholder="Ex: Carthage Eagles">
                </div>

                <div class="form-group">
                    <label class="form-label">Tag (Abbréviation)</label>
                    <input type="text" class="form-input" placeholder="Ex: EGLS" maxlength="5" style="text-transform: uppercase;">
                    <div style="font-size: 0.75rem; color: var(--color-text-muted); margin-top: 0.25rem;">3 à 5 caractères qui apparaîtront à côté de vos pseudos.</div>
                </div>

                <div class="form-group">
                    <label class="form-label">Jeu Principal</label>
                    <select class="form-input">
                        <option>Valorant</option>
                        <option>League of Legends</option>
                        <option>CS:GO 2</option>
                        <option>Autre / Multi-gaming</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Description</label>
                    <textarea class="form-input" rows="4" placeholder="Décrivez votre équipe, vos objectifs..."></textarea>
                </div>

                <div style="margin-top: 2rem; padding: 1.5rem; background: var(--color-bg-tertiary); border-radius: var(--radius-lg); margin-bottom: 2rem;">
                    <h4 style="font-weight: 700; margin-bottom: 0.5rem;">Conditions de Création</h4>
                    <ul style="list-style: none; padding: 0; margin: 0; font-size: 0.875rem; color: var(--color-text-secondary);">
                        <li style="margin-bottom: 0.5rem; display: flex; gap: 0.5rem;">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--color-success)" stroke-width="2"><polyline points="20 6 9 17 4 12"></polyline></svg>
                            Vous deviendrez automatiquement le Leader.
                        </li>
                        <li style="margin-bottom: 0.5rem; display: flex; gap: 0.5rem;">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--color-success)" stroke-width="2"><polyline points="20 6 9 17 4 12"></polyline></svg>
                            Vous pourrez inviter jusqu'à 7 autres joueurs.
                        </li>
                        <li style="display: flex; gap: 0.5rem;">
                             <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--color-success)" stroke-width="2"><polyline points="20 6 9 17 4 12"></polyline></svg>
                            Seul le Leader peut inscrire l'équipe aux tournois.
                        </li>
                    </ul>
                </div>

                <button type="submit" class="btn btn-primary" style="width: 100%; justify-content: center; font-size: 1.125rem; padding: 1rem;">
                    Créer l'Équipe
                </button>
            </form>
        </div>
    </main>
</div>
{% endblock %}
