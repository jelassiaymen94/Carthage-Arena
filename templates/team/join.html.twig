{% extends 'base.html.twig' %}

{% block title %}Rejoindre une &Eacute;quipe - Carthage Arena{% endblock %}

{% block body %}
    <div
      id="mainContent"
      class="flex h-screen w-full opacity-0 transition-opacity duration-500"
    >
      <!-- Sidebar -->
      {% embed 'components/_sidebar.html.twig' %}
        {% block sidebar_bottom %}
        <div class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-gray-900 to-black p-4 border border-white/10">
          <div class="absolute -right-6 -top-6 h-24 w-24 rounded-full bg-primary/20 blur-2xl"></div>
          <div class="relative z-10 flex flex-col gap-3">
            <div class="flex items-center justify-between">
              <p class="text-[10px] font-bold uppercase tracking-widest text-primary">Invitation</p>
              <span class="material-symbols-outlined text-primary text-sm">vpn_key</span>
            </div>
            <div>
              <h4 class="text-sm font-bold text-white">Code d'invitation</h4>
              <p class="text-xs text-gray-400 mt-1">Demandez un code &agrave; un Leader.</p>
            </div>
          </div>
        </div>
        {% endblock %}
      {% endembed %}

      <!-- Main Content -->
      <main class="flex flex-1 flex-col overflow-y-auto bg-background-dark relative">
        <!-- Header -->
        <header class="sticky top-0 z-50 flex h-20 items-center justify-between border-b border-white/5 bg-background-dark/80 backdrop-blur-md px-8 py-4">
          <div class="flex flex-col">
            <h2 class="text-2xl font-bold text-white">Rejoindre une &Eacute;quipe</h2>
            <p class="text-xs text-gray-400">Entrez le code d'invitation fourni par votre Team Leader.</p>
          </div>
          {% include 'components/_header_user_pill.html.twig' %}
        </header>

        <div class="flex flex-col gap-6 p-8 max-w-xl mx-auto w-full">
          <!-- Back Link -->
          <a
            href="{{ path('app_team') }}"
            class="flex items-center gap-2 text-gray-400 hover:text-white transition-colors text-sm font-medium w-fit"
          >
            <span class="material-symbols-outlined text-[18px]">arrow_back</span>
            Retour
          </a>

          <!-- Join Card -->
          <section class="rounded-2xl bg-surface-dark border border-white/5 p-8 text-center">
            <!-- Icon -->
            <div class="flex justify-center mb-6">
              <div class="h-16 w-16 rounded-full bg-white/5 border border-white/10 flex items-center justify-center">
                <span class="material-symbols-outlined text-gray-400 text-[32px]">vpn_key</span>
              </div>
            </div>

            <h3 class="text-xl font-black text-white mb-2">Rejoindre une &Eacute;quipe</h3>
            <p class="text-sm text-gray-400 mb-8">
              Entrez le code d'invitation fourni par votre Team Leader.
            </p>

            <form class="flex flex-col gap-6" method="post" action="{{ path('app_team_join') }}">
              <input type="hidden" name="_csrf_token" value="{{ csrf_token('join_team') }}">

              <div>
                <input
                  type="text"
                  name="invite_code"
                  placeholder="Ex: X7K-9P2"
                  class="w-full bg-[#161616] border border-white/10 rounded-xl py-4 px-4 text-2xl text-center text-white placeholder-gray-600 focus:border-primary focus:outline-none input-glow transition-all font-black tracking-[0.5rem] uppercase"
                />
              </div>

              <button
                type="submit"
                class="w-full rounded-full bg-primary py-3.5 text-sm font-bold text-white shadow-lg shadow-red-900/30 hover:bg-red-600 hover:shadow-red-600/40 hover:-translate-y-0.5 transition-all flex items-center justify-center gap-2"
              >
                <span class="material-symbols-outlined text-[20px]">group_add</span>
                Rejoindre
              </button>
            </form>

            <p class="mt-6 text-xs text-gray-500 leading-relaxed">
              En rejoignant une &eacute;quipe, vous acceptez de suivre ses r&egrave;gles et de participer
              aux tournois inscrits par le Leader.
            </p>
          </section>
        </div>
        {% include 'components/_mobile_nav.html.twig' %}
</main>
    </div>
{% endblock %}
